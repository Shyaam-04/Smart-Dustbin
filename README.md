# Smart-Dustbin
This project transforms a basic dustbin into an Arduino-powered system that listens for sound cues and automatically drives a servo lid and uses an ultrasonic sensor to detect fill percentage, showcasing how simple sensors can create surprisingly smart behaviour.
